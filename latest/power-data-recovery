打开1.33MB的那个PowerDataRecovery,然后在主面板点高级恢复，选中分区，
如果是恢复删除的文件就点打开，红线标注的就是已删除文件
如果是恢复格式化的分区，就点完全扫描，然后把扫描出来的保存到别的分区即可，
扫描出来可能有很多块分区，选中比较大的那个分区大小的，然后操作
不要再在要恢复的分区里写数据，因为可能会覆盖要恢复的数据导致再也恢复不了
注册码 EJVESPWJUYRQZQBYUQPZRPYZEQKHJBVMJYSQVMSE
